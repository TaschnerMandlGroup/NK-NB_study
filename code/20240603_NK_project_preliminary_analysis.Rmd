---
title: "NK project with Magdalena Rados"
author: "Sara Wernig-Zorc"
date: "`r Sys.Date()`"
output: 
  html_document: 
    toc: yes
    toc_depth: 5
    toc_float: yes
    collapsed: false
    code_folding: hide
    highlight: pygments
    theme: spacelab
    df_print: paged
params:
  input: "/home/data/MapMet/"
  out: "/home/out/MapMet/"
  GO_db: "/home/sara_wz/annotation/GSEA_GeneSetEnrichments"
  markers: "/home/sara_wz/annotation/MES_ADRN/"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,
                      tidy = TRUE, 
                      error = FALSE, 
                      messages = FALSE,
                      warning = FALSE)

library(parallel)
future::plan(strategy = 'future::multicore', workers = 12)
options(future.globals.maxSize = 8 * 10^9)

source("/home/sara_wz/MapMet_project/MapMetSC/code/R/CCC_analysis/00_MapMet_color_code.R")

print(params)
```

```{r}
bm <- readRDS("/home/data/CancReg/BM/scBM_healthy.Rds")
nb <- readRDS("/home/out/CancReg/sBM/scBM_healthy.Rds")
```



# __DEG analysis__

<br>


```{r DEG analysis}
# Set groups  to genetic subtypes
Idents(rna) <- rna@meta.data$group

# Normalize the data
rna <- NormalizeData(rna)

# DEG analysis with DElegate
de_results <- DElegate::findDE(object = rna, 
                               group_column = "group")

# DEG analysis with Seurat
#1. Find DE features between ATRXm vs. MYCNA
ATRXm.de.MYCNA <- FindMarkers(rna, ident.1 = "II", ident.2 = "III")
ATRXm.de.MYCNA

#2. Find DE features between ATRTXm vs. sporadic
ATRXm.de.Sporadic <- FindMarkers(rna, ident.1 = "II", ident.2 = "IV")
ATRXm.de.Sporadic

#3. Find DE features between ATRTXm vs. non-DTC-infiltrated BM
ATRXm.de.Ctrl <- FindMarkers(rna, ident.1 = "II", ident.2 = "I")
ATRXm.de.Ctrl
```

Here we want to know which genes are differentially expressed in NK cells in 
ATRX deleted NB genetic subtype, compare to other genetic subtypes (MYCN amplified 
and sporadic) and in control BM.

```{r NK cells only}
# Set groups as genetic groups
Idents(rna) <- rna@meta.data$original_celltype

# subset rna object to NK cell cluster 
NK_cells <- "NK (4)"
fetahu.NK <- subset(rna, idents = NK_cells, invert = FALSE)

# Set groups to genetic subtypes
Idents(fetahu.NK) <- fetahu.NK@meta.data$group

# Plot UMAP with 
DimPlot(fetahu.NK)

#0. Normalize expression data
rna <- NormalizeData(fetahu.NK)


#1. DEG analysis with DElegate
de_results <- DElegate::findDE(object = fetahu.NK, 
                               compare = "all_vs_all", 
                               order_results = TRUE,
                               meta_data = metadata,
                               compare_is_ref = TRUE,
                               group_column = "group")

de_results
write.table(de_results, file = paste0(params$out,"/tables/DEG_ATRXvsAll_DElegate.csv"),
            sep = "\t", row.names = FALSE, col.names = TRUE)

#2. DEG analysis with Seurat
#2.1 Find DE features between ATRXm vs. MYCNA
ATRXm.de.MYCNA.NK <- FindMarkers(fetahu.NK, ident.1 = "III", ident.2 = "II")
ATRXm.de.MYCNA.NK
write.table(ATRXm.de.MYCNA.NK, file = paste0(params$out,"/tables/DEG_ATRXvsMYCNA.csv"),
            sep = "\t", row.names = TRUE, col.names = TRUE)

#2.2 Find DE features between ATRTXm vs. sporadic
ATRXm.de.Sporadic.NK <- FindMarkers(fetahu.NK, ident.1 = "III", ident.2 = "IV")
ATRXm.de.Sporadic.NK
write.table(ATRXm.de.Sporadic.NK, file = paste0(params$out,"/tables/DEG_ATRXvsSporadic.csv"),
            sep = "\t", row.names = TRUE, col.names = TRUE)

#2.3 Find DE features between ATRTXm vs. non-DTC-infiltrated BM
ATRXm.de.Ctrl.NK <- FindMarkers(fetahu.NK, ident.1 = "III", ident.2 = "I")
ATRXm.de.Ctrl.NK
write.table(ATRXm.de.Ctrl.NK, file = paste0(params$out,"/tables/DEG_ATRXvsCtrl.csv"),
            sep = "\t", row.names = TRUE, col.names = TRUE)
```

